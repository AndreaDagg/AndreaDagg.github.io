{
  "name": "andrea-daguanno-portfolio",
  "private": true,
  "version": "3.0.0",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "node scripts/prepare-assets.mjs && vite build && node scripts/verify-build.mjs",
    "preview": "vite preview --host 127.0.0.1",
    "photos": "node scripts/prepare-assets.mjs",
    "lint": "eslint .",
    "test": "node --test tests/*.test.mjs",
    "test:browser": "playwright test"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.63.0",
    "eslint": "^10.11.0",
    "globals": "^17.12.0",
    "sharp": "^0.35.4",
    "vite": "^8.3.1"
  }
}
